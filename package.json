{
  "name": "book-adventure",
  "version": "1.0.0",
  "private": true,
  "description": "Transform PDF books into playable text adventure games",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "generate": "npm run generate -w @book-adventure/generator --",
    "build:runtime": "npm run build -w @book-adventure/runtime",
    "dev": "npm run build:runtime && cp games/*.json packages/runtime/dist/ && python3 -m http.server 8080 -d packages/runtime/dist",
    "deploy": "./scripts/deploy.sh"
  },
  "keywords": [
    "text-adventure",
    "interactive-fiction",
    "pdf",
    "game-engine"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "terser": "^5.46.0"
  }
}
